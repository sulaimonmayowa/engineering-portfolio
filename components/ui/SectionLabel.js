export default function SectionLabel({ children, className = '' }) {
  return (
    <span className={`font-label-mono text-label-mono text-secondary ${className}`}>
      // {children}
    </span>
  );
}
